name: CI/CD Deploy to Droplet

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to droplet via SSH
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.DROPLET_SSH_KEY }}

      - name: Deploy to droplet
        run: |
          ssh -o StrictHostKeyChecking=no ${{ secrets.DROPLET_USER || 'root' }}@${{ secrets.DROPLET_IP }} << EOF
            mkdir -p /home/ubuntu/app/SellersGPT
            cd /home/ubuntu/app/SellersGPT

            # Pull latest code
            git fetch --all
            git reset --hard origin/main

            # Ensure Docker Compose installed
            if ! command -v docker >/dev/null 2>&1; then
              curl -fsSL https://get.docker.com | sh
            fi
            if ! docker compose version >/dev/null 2>&1; then
              sudo apt update && sudo apt install -y docker-compose-plugin
            fi

            # Create .env.production if missing
            if [ ! -f .env.production ]; then
              echo "${{ secrets.PROD_ENV_FILE }}" > .env.production
            fi

            # Start Docker Compose
            docker compose down
            docker compose up -d --build

            # Cleanup old images
            docker image prune -f
          EOF
